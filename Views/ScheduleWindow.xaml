<Window x:Class="DevicesControlApp.Views.ScheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đặt lịch hẹn giờ" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Thiết bị:" Width="100"/>
            <ComboBox x:Name="cbDevices" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="1">
            <Label Content="Hành động:" Width="100"/>
            <ComboBox x:Name="cbAction" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="2">
            <Label Content="Ngày thực hiện:" Width="100"/>
            <DatePicker x:Name="dpTime" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="3">
            <Label Content="Giờ:" Width="100"/>
            <TextBox x:Name="txtHour" Width="50" Text="12"/>
            <Label Content=":"/>
            <TextBox x:Name="txtMinute" Width="50" Text="00"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0" Grid.Row="3">
            <Button Content="Đặt lịch" Width="80" Margin="5" Click="btnAddSchedule_Click"/>
            <Button Content="Hủy" Width="80" Margin="5" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
